import pandas as pd 

msg=pd.read_csv("")
print(msg.shape)

msg=msg.label.map({'pos':1,'neg':0})

x=msg.message
y=msg.labelnum
print(x)
print(y)

from sklearn.model_selection import train_test_split
x_train , y_train ,x_test, y_test=train_test_split(x,y)
print(y_train)
print(y_test)

from sklearn.feature_extraction import CountVectorizer
cv=CounterVectorizer()
xtr=cv.fir_transform(x_train)
xt=cv.tranform(x_test)
print(cv.get_feature_names())
df=pd.dataframe(xtr.toarrey,coloumn=cv.get_feature_names())

from sklearn.naive_bayes import MutinomialNB
mn=Multinomial().fit(xtr,Y_train)
pre=mn.predict(xt)

print(X_train)
print(pre)

from sklearn import Metrics
print(Metrics.accuracy_score(Y_train,pre))
print(metrics.confusion_metrics(Y_train,pre))
print(metrics.recall_score(Y-train,pre))
