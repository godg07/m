import pandas as pd
import numpy as np

data=pd.read_csv("")
print (data)

concept =np.array(data)[:,:-1]
print(concept)

tartget=np.array(data)[:,-1]
print(tartget)

def train(con, tar)

for i ,valenumerate (tar)
if val=='yes':
    
specific_h=con[i].copy()

break

for i,val enumerate (con)
 if tar[i]=='yes':
     for x in range (len(specif_h))
     if val[x]!=sepecific_h[x]:
         specific_h[x]='?'
         else:
             pass
         return specific_h
         
         print(train(concept,target))
